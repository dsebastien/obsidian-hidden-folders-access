# 2026-07-17

Ported the fixed two-phase release workflow from obsidian-remarkable-sync.

## Accomplished

- Replaced `.github/workflows/release.yml` verbatim with the two-phase design: a "prepare" job (dispatch on a branch) bumps/commits/tags with a build gate, then re-dispatches the workflow at the tag; a "publish" job builds, attests, and creates the release at the tag commit. This fixes the community-catalog rejection where the build-provenance attestation was bound to a different commit than the release tag (proven live via remarkable-sync 1.5.0).
- Kept `bun test` in the prepare-job build gate (69 tests pass). All referenced scripts (`release:update-version`, `release:version-bump`, `release:changelog`) and committed files (including `docs/release-notes.md`, generated by `scripts/generate-changelog.ts`) exist here, so no adaptation was needed.
- Updated `scripts/release.sh` workflow-summary text to describe the new two-phase flow.
